# 2026-09-17 工作日志

## 站内跳转改为异步局部加载（用户诉求：切换页面"没反应"像断网）

### 根因
站内跳转在 PocketBay 的 embed 帧内是**整页导航**。请求一旦被挂住（应用被平台休眠 /
冷启动 / 服务端首次计算行情），旧界面完全静止 → 用户以为网断了。上一版只加了遮罩
提示，跳转本身仍是整页刷新，慢的时候依旧静止。

### 落地
- `base.html` 引入两个交换边界：`#page-root`（内容，包住 `.container`）、
  `#page-scripts`（页面脚本）。**必须分开** —— `screening.html` 的 `<script>` 写在
  content 块里；且 `innerHTML` 插入的 `<script>` 不执行，需手动按块重放。
- 新增 `asyncNav` 引擎：拦截站内链接 → 立即加载态 → `fetch` 目标页 HTML →
  只替换内容区 → 按序重放页面脚本 → `pushState` + 自行更新导航高亮。
- 加载态：顶部不确定进度条 + 内容淡化（旧内容保留不白屏）+ 分阶段文案
  （正在加载 → 正在计算行情数据 → 应用可能正在唤醒，带已等待秒数）+ 60s 安全阀。
- 悬停预取：260ms 防抖、单飞串行、10s 命中窗口。
- 任何环节不符合预期 → 回退整页导航；表单提交语义不变，只补反馈。

### 两个必须记的技术点
1. **页面脚本顶层 `let`/`const` 是致命陷阱**：同一文档二次注入会抛
   `Identifier 'x' has already been declared` → **整个脚本不执行**，复访该页
   功能静默失效（不报错到页面、只在控制台）。`control.html` / `reports.html` 命中。
   解法：`deLexicalize()` 把**行首**（零缩进）let/const 降级为 `var`
   （函数体内块级作用域不动，语义不变）；另挂一次性 error 守卫，命中确定性
   SyntaxError 就回退整页导航。
2. **PocketBay 的 embed 帧内 `fetch()` 能拿回应用 HTML**：网关按 `Sec-Fetch-Dest`
   分流，XHR/fetch 是 `empty`，走应用而非外壳页。所以异步局部加载在平台上成立
   —— 这是本次能做的前提，已用真实浏览器在线上确认。

### 验证（全部真实浏览器，不是 HTTP 探测）
- 本地：`async_nav_verify.js` **31/31**（不整页刷新 / 脚本重放 / 含顶层 const 页面
  二次访问 / 后退前进 / 接管范围 / 慢响应期间加载态可见且文案推进 / 失败兜底 /
  控制台无报错）
- 线上：`live_async_nav.js` **17/17**。点击「信号」598ms、`/recommendations`
  **70ms（预取命中）**——同一页此前冷启动要 10.2s。
- `pytest` **72 passed**（原 56 + 新增 16）；四套 E2E 5/5、12/12、7/7、28/28。
- 新增 `tests/test_web/test_partial_nav.py`：守住交换边界 + 冻结行首 let/const 清单。

### 工具坑（本轮浪费了不少时间）
- **同一条消息里对同一文件发多个 Edit 会相互覆盖**，且工具仍报"成功"。
  `.container` 的包裹就是这样被静默吞掉的（`#page-scripts` 保住了、`#page-root` 没了），
  表现是浏览器测试首屏就全线 FAIL。**同一文件的多处改动必须逐条串行发送**，
  改完用 Grep 复核关键标记。
- 控制台那句 `Failed to load resource: 404` **不含 URL**，不能用来判断归属；
  要用 `page.on("response")` 或 `m.location().url` 按 URL 归属。线上唯一 4xx 是
  PocketBay **外壳页**请求 `/favicon.ico`（应用侧 `--e` 零 4xx）。

### 交付
- commit `28c974e`（4 files，+479/-30）
- 线上 <https://aitrader.pocketbay.app>（`deployment_id=8724`，`running`）
- 技能 `~/.workbuddy/skills/pocketbay-deploy/SKILL.md` 已补充"异步局部加载"方案与
  embed 帧 fetch 可行性。

## 首次推送 GitHub 远端

- 新增 remote `origin` = <https://github.com/freeEasyLoope/AiTraderPlatform.git>，
  `main` 已建立 upstream 跟踪。
- 首次推送即全量 17 个 commit；远端 HEAD 与本地一致 `365aa89`。
- 远端此前是**空仓库且匿名可读（公开）** → 推送前已用 `git grep` 全量扫过
  `password|token|secret|api_key|PRIVATE KEY`：命中的全是**环境变量名**
  （`ANTHROPIC_API_KEY` / `TUSHARE_TOKEN` / `AITRADER_PASSWORD`）与 `.env.example`
  占位符，无真实密钥。`data/*.db`、`logs/*`、`*.pyc` 由 `.gitignore` 挡住。
- 值得注意：仓库里 `agent` 工具目录（`.claude/`、`.agents/`、`.impeccable/`）
  是**已跟踪状态**且占了大部分体积（impeccable 技能脚本 ~1MB）——若要瘦身需
  `git rm -r --cached` 后加 `.gitignore`，属于改写历史的操作，未擅自执行。
- 本机 git 凭据走 Windows Credential Manager（`credential.helper=manager`）已缓存，
  非交互推送直接成功。非交互推送统一加 `GIT_TERMINAL_PROMPT=0 GCM_INTERACTIVE=never`，
  避免无凭据时挂死。

## 修「加载提示永不消失」与「漏斗点不进去」（用户反馈）

线上两个症状，根因都是「不报错、不 500、只是体验坏掉」，此前所有测试都通过。

### ① 加载提示永久盖屏
`#navLoading` 由 JS 创建后长期复用，`hideLoading()` 只做「移除 `.on`」，
**但 CSS 里根本没有 `#navLoading.on` 规则，基态又是可见的**
（`position:fixed` 全屏 + `rgba(0,0,0,.55)` + `blur(2px)`）。
→ 去掉类名毫无视觉变化，遮罩从第一次跳转起永久留着，整站像蒙了一层。
教训：**断言类名 ≠ 断言计算样式**。之前的 E2E 只检查 `classList` 不含 `on` 就判通过，
所以 31/31 全绿也没发现。现在改为看 `getComputedStyle` 的 `visibility/opacity`。
改法：基态 `visibility:hidden` + `opacity:0`（visibility 用 0s 延迟过渡做淡出），
`.on` 才显示；并**去掉全屏深色模糊**（把"加载慢"放大成"网站挂了"），
改成导航栏下方（`top:68px`）居中的胶囊提示，内容区淡化从 0.38 放宽到 0.5。

### ② 漏斗 307 → 明文 http → 被浏览器按混合内容拦掉
`screening.py` 用了 `APIRouter(prefix="/screening")` + `@router.get("/")`，
真实路径是 `/screening/`，访问 `/screening` 得到
`307 Location: http://aitrader--e.pocketbay.app/screening/`。
应用在平台 https 网关后（未传 `X-Forwarded-Proto` 时 Starlette 按明文 http 生成绝对 URL），
https iframe 内跟随该重定向会被拦 → 表现「点了没反应 / 进不去」。
**全站只有这一个页面带尾斜杠**。改法：去掉 prefix、路径写全量
（与 `/signals`、`/reports` 一致），`/screening` 与 `/screening/` 都直接 200。
教训：**导航项目标一律不允许依赖 3xx**，整页刷新会掩盖它、fetch 局部加载不会。

### ③ 平台边缘缓存导致「新页面 + 旧 CSS」
修完部署后线上 `/static/style.css` 仍返回旧内容（`age=1305`、43400B，包内已 45000B）。
原因：应用对 `/static/*` 下发 `max-age=14400`，平台边缘节点照此缓存；
而动态 HTML 是 `no-store` 每次新的 → **最长 4 小时内样式改了却不生效**，
且因为 HTML 确实变了，极容易被误判成"改动没起作用"。
改法：`deps.py:static_version()`（style.css 的 sha256 前 12 位，进程内缓存）
注册成 Jinja 全局，`base.html` 引用 `/static/style.css?v=<指纹>`。
**以后凡是改 `/static/` 下的资源，必须确认引用处带指纹。**

### 加固（asyncNav）
- `fetchHTML` 连 `resp.url`（跟随重定向后的最终地址）一起返回，地址栏以它为准
- 路径尾斜杠归一化 `normPath()`：`/screening` 与 `/screening/` 视为同一页
- **兜底整页导航也被拦住时**（网关策略 / `location.assign` 抛错）：
  4s 探针发现文档没变就把加载提示转成可操作态（「重试 / 刷新整页」），
  并解除导航锁 —— 不再永远停在加载态让用户以为网断了。
  注意：探针必须先挂再导航，否则 `assign` 抛错就再没东西能救回页面。

### 验证
- 本地真实浏览器：8 个导航项全部 ✅（加载态消失、遮罩不可见、内容真的换了）
- 线上真实浏览器（走外壳页 → `--e` 帧）：8 项全 ✅，漏斗页 `runBtn=true`
- 线上 4xx/5xx **零**；唯一异常是平台自己的 `pocketbay.com/api/analytics/collect`
  （ERR_ABORTED），应用侧 `--e` 零异常
- `pytest` **75 passed**（新增 3 条：CSS 基态隐藏契约、导航项不得 3xx、样式表必须带指纹）
  - 已验证新 CSS 测试在 HEAD 版 `style.css` 上会 **FAIL**，是真守卫
- commit `88276ad`（修复）+ `f2df4aa`（指纹）+ `0b63f43`（清理误提交的临时文件）
- 部署 `deployment_id=8727`（修复）→ `8728`（指纹），`aitrader.pocketbay.app` running

### 操作失误记一笔
`git add -A` 前忘了删掉用来接命令输出的 `_p.txt`，被一起提交并推送，只能补一个
清理提交。**本机 bash 缺 coreutils（无 `rm`/`ls`/`cat`/`sleep`），我惯用
`> _x.txt` 再 Read 的方式看输出** —— 这类临时文件在提交前必须先删干净。
